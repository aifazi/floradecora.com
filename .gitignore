# dependencies
node_modules/
floradecora/node_modules/
floradecora/.next/
floradecora/out/
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem
*.log
dev.log
dev.error.log

# env - keep secrets ignored but allow examples
.env
.env*.local
.env.local
!.env.example
!.env.coolify.example
!floradecora/.env.local.example
!backend/.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# docker
.docker/

# wsl
.wsl/

# assest raw — served from R2 CDN https://cdn.aifazi.net/media/assest/
# Untracked via `git rm -r --cached assest` (446 MB) — keep local files but do not commit.
# To fully purge history, run: git filter-repo --path assest --invert-paths
assest/
.vercel