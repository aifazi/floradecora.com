# dependencies
node_modules/
floradecora/node_modules/
floradecora/.next/
floradecora/out/
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem
*.log
dev.log
dev.error.log

# env
.env
.env*.local
.env.local

# typescript
*.tsbuildinfo
next-env.d.ts

# docker
.docker/

# wsl
.wsl/

# assest raw - served from R2 CDN https://cdn.aifazi.net/media/assest/ — use CDN only, not git
# assest/ is kept in history but ignored for new files; run `git rm -r --cached assest` to stop tracking if repo too large
# For local optimized copies, webp is preferred (6.7MB vs 46MB). Keep both png + webp for fallback.
# To fully untrack 446MB assest folder: uncomment next line and run git rm -r --cached assest
# assest/

# git
.git/
