# Supabase Postgres (get from Supabase Dashboard -> Database -> Connection string)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres"
# Admin API key for protected routes (legacy fallback, will be deprecated after JWT)
ADMIN_API_KEY="change-me-to-a-strong-random-secret"
# JWT — required for admin auth (generate with: openssl rand -base64 32)
JWT_SECRET="change-me-to-a-strong-32-char-secret-use-openssl-rand-base64-32"
# R2 / S3 (for media)
R2_ENDPOINT="https://YOUR_ACCOUNT.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID="your-r2-access"
R2_SECRET_ACCESS_KEY="your-r2-secret"
R2_BUCKET="floradecora"
CDN_URL="https://cdn.aifazi.net"
# App
PORT=3002
CORS_ORIGIN="https://floradecora.com,http://localhost:3000,http://localhost:3001"
# Turnstile (optional)
TURNSTILE_SECRET_KEY=""
# Web3Forms fallback (if not using DB for contact)
WEB3FORMS_KEY=""
